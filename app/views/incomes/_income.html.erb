<div class="container">
	<div class="incomename">
	<%= income.amount %>
	<%= income.from %>
	<%= income.date %></span>
	<%= link_to "Delete", [income.month, income], method: :delete, data: {confirm: "Are you sure you want to delete this income?"}, class: "btn btn-danger" %>
	<p><%= time_ago_in_words(income.created_at) %> Ago</p>
	</div>
</div>